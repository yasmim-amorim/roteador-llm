# Copie este arquivo para ".env" e preencha quando chegarmos na Fase 5.
# O ".env" real nunca é commitado (está no .gitignore).
# Exemplo (o provedor exato ainda será escolhido com você na Fase 5):
# GROQ_API_KEY=
